template_path: templates/vpc_wrapper.py

hooks:
  before_create:
    #- !route53_hosted_zone sceptremods.example.com
    - !route53_hosted_zone sceptremods.devops.ucop.edu
  before_update:
    #- !route53_hosted_zone sceptremods.example.com
    - !route53_hosted_zone sceptremods.devops.ucop.edu

stack_tags:
  seg:team: syseng
  seg:app: sceptremods
  seg:env: test

# Build as single public subnet named Ashley in a single Availablility Zone.
sceptre_user_data:
  VpcCIDR: 10.130.0.0/16
  AZCount: 1
  UseDefaultSubnets: False
  CustomSubnets:
    Ashely:
      net_type: public
      priority: 2
